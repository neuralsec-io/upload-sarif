[tools]
go = "1.27.0"
prek = "0.4.14"
"github:golangci/golangci-lint" = "2.13.1"
"github:goreleaser/goreleaser" = "2.17.1"
"github:gotestyourself/gotestsum" = "1.13.0"
# Exclude is version-exact so anything else still trips mise's trust policy.
"npm:renovate" = { version = "44.39.1", trust_policy_excludes = [
  "@yarnpkg/libzip@3.2.2",
] }

[tasks.format]
run = "golangci-lint fmt"
description = "Format code"

[tasks.lint]
run = "golangci-lint run"
description = "Run lint tools"

[tasks.install]
run = "go mod download"
description = "Download dependencies"

[tasks.freeze]
run = "go mod tidy && go mod verify"
description = "Freeze dependencies"

[tasks."freeze:upgrade"]
run = "go get -u ./... && go mod tidy && go mod verify"
description = "Upgrade dependencies"

[tasks.test]
run = "gotestsum --format testdox --format-hide-empty-pkg ./..."
description = "Run tests"

[tasks.snapshot]
run = "goreleaser build --clean --snapshot --single-target -o \"dist/upload-sarif\""
description = "Build binary in snapshot mode"
