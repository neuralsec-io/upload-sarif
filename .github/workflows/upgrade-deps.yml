name: Upgrade deps

on:
  workflow_dispatch:
  schedule:
    - cron: "0 10 * * 1-5"

permissions: {}

concurrency:
  group: upgrade-deps
  cancel-in-progress: false

jobs:
  renovate:
    # Run locally rather than via a shared workflow; update policy still comes
    # from the preset in .github/renovate.json, so only the runner is local.
    runs-on: ubuntu-latest
    permissions:
      contents: read # checkout only; Renovate authenticates as the App below
    steps:
      - id: token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: 1988247
          private-key: ${{ secrets.UPGRADE_DEPENDENCIES_BOT_PEM }}
          owner: neuralsec-io
          # Narrowed from the App's full installation scope: the repo being
          # updated, plus the one holding the preset Renovate resolves.
          repositories: |
            upload-sarif
            upgrade-deps

      # persist-credentials: false — the header checkout leaves behind is scoped
      # to this repo and would override Renovate's own git credentials.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          install: true
          cache: false

      - name: Run Renovate
        env:
          RENOVATE_BINARY_SOURCE: global # tools come from mise, not containerbase
          RENOVATE_REPOSITORIES: ${{ github.repository }}
          RENOVATE_ONBOARDING: "false"
          RENOVATE_PLATFORM_COMMIT: "enabled" # commits signed by the App
          RENOVATE_CONFIG_FILE: .github/renovate.json
          RENOVATE_TOKEN: ${{ steps.token.outputs.token }}
        # node@24 rather than whatever the runner image ships: renovate 44
        # declares `node ^24.11.0`, and ubuntu-latest is already past that.
        run: mise exec node@24 -- renovate
